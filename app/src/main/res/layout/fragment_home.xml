<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:fillViewport="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="16dp"
        android:background="@color/cool_gray">

        <!-- Main SOS Button -->
        <Button
            android:id="@+id/sosButton"
            android:layout_width="180dp"
            android:layout_height="180dp"
            android:layout_marginBottom="20dp"
            android:background="@drawable/sos_button"
            android:elevation="8dp"
            android:text="@string/emergency_n_sos"
            android:textColor="@android:color/white"
            android:textSize="22sp"
            android:textStyle="bold"
            app:backgroundTint="@null" />

        <!-- Horizontal layout for Cancel SOS and Right Button -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center"
            android:layout_marginBottom="20dp"
            android:layout_marginTop="0dp">

            <!-- Cancel SOS button on left -->
            <Button
                android:id="@+id/see_sos"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:layout_marginEnd="8dp"
                android:background="@drawable/home_bg"
                android:text="@string/view_sos"
                android:textColor="@android:color/black"
                android:textSize="16sp"
                app:backgroundTint="@null" />

            <!-- Another button on right -->
            <Button
                android:id="@+id/add_alert"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:layout_marginStart="8dp"
                android:background="@drawable/home_bg"
                android:text="@string/add_alert"
                android:textColor="@android:color/black"
                android:textSize="16sp"
                app:backgroundTint="@null" />

        </LinearLayout>

        <TextView
            android:id="@+id/activesos"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/active_sos"
            android:layout_marginBottom="20dp"
            android:layout_marginStart="5dp"
            android:textSize="24sp"
            android:textColor="@color/black"
            android:textStyle="normal|bold" />
        <!-- RecyclerView for showing active SOS -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/activeSosRecyclerView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:nestedScrollingEnabled="false"
            android:overScrollMode="never"
            android:layout_marginBottom="20dp"
            tools:ignore="DuplicateIds" />
        <TextView
            android:id="@+id/resolves_sos"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/resolved_sos"
            android:layout_marginBottom="20dp"
            android:layout_marginStart="5dp"
            android:textSize="24sp"
            android:textColor="@color/black"
            android:textStyle="normal|bold" />
        <!-- RecyclerView for showing active SOS -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/resolvedSosRecyclerView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:nestedScrollingEnabled="false"
            android:overScrollMode="never"
            android:layout_marginBottom="20dp" />
    </LinearLayout>


</ScrollView>
